{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Run Simulation",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/tp1/main.py",
            "console": "integratedTerminal",
            "justMyCode": true,
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}
